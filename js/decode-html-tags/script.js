// DISPLAY TAGS (<>) BUT ENCODED, TO AVOID EXECUTION OF CODE
function escHTML(text) {
    var map = {
        '&': '&amp;',
        '<': '&lt;',
        '>': '&gt;',
        '"': '&quot;',
        "'": '&#039;'
    };
    return text.replace(/[&<>"']/g, function(m) { return map[m]; });
}
